spring.application.name=PersonalFinanceInsightAI
spring.datasource.url=jdbc:mysql://localhost:3306/pfia_db
spring.datasource.username=root
spring.datasource.password=12345678
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
# After changing an already-applied migration file, Flyway validation fails until you run repair on the DB.
# Default false for local convenience; set SPRING_FLYWAY_VALIDATE_ON_MIGRATE=true in production deployments.
spring.flyway.validate-on-migrate=${SPRING_FLYWAY_VALIDATE_ON_MIGRATE:false}
app.jwt.secret=${JWT_SECRET:replace-with-a-very-long-32-byte-secret-key}
app.jwt.expiration=86400000
app.frontend.url=${APP_FRONTEND_URL:http://localhost:5173}
gemini.api-key=${GEMINI_API_KEY:}
gemini.model=gemini-2.5-flash
gemini.base-url=https://generativelanguage.googleapis.com
gemini.timeout-seconds=15
gemini.max-retries=2
app.google.client-id=${GOOGLE_CLIENT_ID:49189547110-bm5sudeobvf6diik6fpn9cf81u3n97aq.apps.googleusercontent.com}
app.mail.from=${MAIL_FROM:tiendungx832005@gmail.com}
app.mail.fallback-log-otp=${MAIL_FALLBACK_LOG_OTP:false}
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:tiendungx832005@gmail.com}
spring.mail.password=${MAIL_PASSWORD:zjuewzfrdgvapfwf}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
